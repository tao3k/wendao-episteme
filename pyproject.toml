[project]
name = "wendao-episteme"
version = "0.1.0"
description = "Source ontology contracts and deterministic validation tools for Wendao Episteme."
requires-python = ">=3.11"
dependencies = [
  "pyarrow>=23.0.0",
]

[tool.uv]
package = false
